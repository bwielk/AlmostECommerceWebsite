import React from "react"

const Header = (props) => {
    return(
      <nav className = "navbar">
      <div className = "nav-container">
      <ul>
      <li><a href = "#">HOME</a></li>
      </ul>
      </div>
      </nav>
      );
  }

  export default Header;